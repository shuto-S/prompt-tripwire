# Judge fixture instructions

- Keep this fixture local and dependency-free.
- Change only `src/greeting.js` and `test/greeting.test.js`.
- Use `npm test` as the verification command.
- Do not access the network, add packages, modify Git history, or perform external actions.
